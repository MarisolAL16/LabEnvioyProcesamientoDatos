# frontend\src\components\Navbar.jsx

```js
export default function Header() { // exporta el valor principal de este archivo.
  return ( // devuelve un valor y termina esta parte de la funcion.
    <nav className="navbar navbar-dark bg-primary"> // muestra un componente de React.
      <div className="container"> // muestra un componente de React.
        <span className="navbar-brand"> // muestra un componente de React.
          <i className="bi bi-car-front-fill me-2"></i> // muestra un componente de React.
          Sistema de Parqueo // parte de un componente visual reutilizable.
        </span> // muestra un componente de React.
      </div> // muestra un componente de React.
    </nav> // muestra un componente de React.
  ); // abre o cierra bloques de codigo.
} // abre o cierra bloques de codigo.
```
