# frontend\src\components\Footer.jsx

```js
export default function Footer() { // exporta el valor principal de este archivo.
  return ( // devuelve un valor y termina esta parte de la funcion.
    <footer className="bg-dark text-white text-center py-3 mt-5"> // muestra un componente de React.
      <div className="container"> // muestra un componente de React.
        <p className="mb-0"> // muestra un componente de React.
          <i className="bi bi-car-front-fill me-2"></i> // muestra un componente de React.
          Sistema de Parqueo Â© 2026 // parte de un componente visual reutilizable.
        </p> // muestra un componente de React.
// se deja espacio para separar secciones del codigo.
        <small> // muestra un componente de React.
          Desarrollado con React, Express y Bootstrap // parte de un componente visual reutilizable.
        </small> // muestra un componente de React.
      </div> // muestra un componente de React.
    </footer> // muestra un componente de React.
  ); // abre o cierra bloques de codigo.
} // abre o cierra bloques de codigo.
```
